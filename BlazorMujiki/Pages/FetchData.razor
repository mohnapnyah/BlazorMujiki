@page "/fetchdata"

<PageTitle>Mujiki</PageTitle>

@using BlazorMujiki.Data
@inject SingletonService singleton1

@{
    if (singleton1.GetUser().Count == 0)
    {
        singleton1.SetUser(MongoExample.FindAllUnits());
    }
}
<Users Units="singleton1.GetUser()"></Users>
